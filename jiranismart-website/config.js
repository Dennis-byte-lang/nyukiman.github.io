// Website runtime configuration.
// Set apiBaseUrl to the same backend the mobile app uses.
// Examples:
// 1) Empty => auto uses current origin + /api (when served by backend)
// 2) "http://localhost:5000/api"
// 3) "http://192.168.1.10:5000/api" (LAN / phone testing)
window.JIRANI_CONFIG = {
  apiBaseUrl: "",
};
